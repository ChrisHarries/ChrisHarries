aws_region = "eu-north-1"
project    = "claude-terraform"

# These must be unique email addresses not already associated with an AWS account.
# AWS uses them as the root login for each member account.
prod_account_email = "REPLACE_WITH_PROD_EMAIL"
dev_account_email  = "REPLACE_WITH_DEV_EMAIL"
